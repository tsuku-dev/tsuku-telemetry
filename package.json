{
  "name": "tsuku-telemetry",
  "version": "1.0.0",
  "description": "Cloudflare Worker for tsuku CLI telemetry collection",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.93.0"
  }
}
